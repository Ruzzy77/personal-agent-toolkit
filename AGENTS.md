# Personal Agent Toolkit 작업 지침

## 한국어 표현

Sense의 `conversation-and-writing` 섹션과 연결된 `korean-generation` 스킬을 따른다. 필요한 전문은 조회하고, 이미 읽은 같은 버전은 재사용한다.

- Toolkit과 Flow의 메뉴, 버튼, 안내문, 오류 메시지, 예시 콘텐츠 등 사용자에게 보이는 한국어 전체에 적용한다.
- 문구를 정하기 전에 사용자의 상황과 목적, 기능 실행 후 달라지는 내용을 파악한다. 사용자가 알아야 할 내용과 화면에서 이미 드러나는 내용을 구분해 무엇을 명시하고 생략할지 정한다.
- 실제 기능과 사용 맥락에 맞춰 처음부터 한국어로 문구를 작성한다. 영문 UI 명칭이나 코드 용어를 그대로 옮겨 문구를 정하지 않는다. 통용되는 전문어와 공식 명칭은 유지하고, 독자에게 익숙한 용어를 불필요하게 풀어 쓰지 않는다.
- 간결하고 전문적인 한국어를 기본으로 한다. 메뉴와 버튼은 대상과 실제 동작을 분명히 나타내고, 실행 후 무엇이 달라지는지 예상할 수 있게 쓴다. 안내문과 예시 콘텐츠는 목적과 독자에 맞는 문체로 쓴다.
- 표현을 다듬을 때는 대상과 동작, 조건, 결과의 의미와 가능성의 정도를 유지한다.
- 표현을 지적받으면 유의어 교체로 끝내지 않는다. 전하려는 뜻에 맞춰 문장을 다시 쓰고, 같은 문제가 있는 관련 문구도 함께 고친다. 이미 자연스럽게 쓰인 표현은 보존한다.

## 화면 설계

- 화면을 새로 만들거나 간격, 크기, 배치, 정렬을 바꿀 때에는 설치된 UIKit design 스킬의 references/layout.md를 먼저 적용한다. 원본은 UIKit 저장소의 skills/design/references/layout.md이며 이 저장소에 사본을 두지 않는다.
- 설치된 UIKit의 README, 토큰, 컴포넌트 API와 기존 사용처를 확인한다. 공식 컴포넌트의 내부 규격은 제공된 옵션으로 정하고, 화면 코드는 콘텐츠의 관계와 부모 배치를 맡는다.
- 독립된 실행 버튼은 공식 컴포넌트의 테두리나 배경으로 클릭 영역을 드러낸다. 텍스트만 놓거나 마우스를 올렸을 때만 경계가 나타나는 버튼은 기본값으로 쓰지 않는다. 메뉴 항목, 목록 행, 경로 이동과 아이콘 툴바는 해당 구성의 구분 방식을 따른다.
- 공통 배치 클래스와 의미 기반 토큰을 우선 사용한다. 같은 역할에 다른 수치를 임의로 추가하거나, 잘림과 정렬 오류를 개별 위치 보정으로 감추지 않는다.
- 외부 레퍼런스는 구성 원리를 참고한다. Geist나 OpenAI 예시의 수치와 스타일로 현재 UIKit을 덮어쓰지 않는다.
- 변경한 영역은 실제 한국어 콘텐츠로 큰 화면과 작은 화면에서 확인한다. 관련 breakpoint, 긴 문구와 조작 상태도 확인하며, 토큰 사용이나 빌드 성공만으로 시각 점검을 대신하지 않는다.
