# Personal Agent Toolkit 설계

이 문서는 제품 사이의 실행 경계와 통일 방향을 정하는 저장소 정본이다. 현재 제품 목록, 공개
MCP 표면과 release version은 [`products.json`](./products.json)에 둔다. 제품별 의미와 동작은
각 `plugins/<product>/DESIGN.md`가 맡는다.

## 설계 목표

- 같은 종류의 제품은 같은 흐름으로 배포하고 운영한다.
- 상태가 있는 원격 제품은 서비스가 데이터와 업무 규칙을 소유한다.
- Site와 MCP는 서로를 중계하지 않고 같은 제품 서비스를 사용한다.
- plugin은 연결, Skill과 정적 UI 자산만 배포하며 운영 데이터나 개인 자산을 소유하지 않는다.
- OpenAI에서는 원격 제품을 등록 app 하나로 연결하고 문서 Skill을 같은 정본에서 배포한다. Codex는
  이를 marketplace plugin 하나로 설치하고, 비공개 개인 ChatGPT는 등록 app과 Personal Skills를 쓴다.
  Claude에서는 제품별 원격 MCP와 Document Files local MCP를 사용한다.
- 공통 코드는 실제로 반복되는 인증·계약·오류 처리부터 합치고, 제품 차이를 감추는 추상화는
  만들지 않는다.

## 제품 지도

| 제품 | 구성 | 데이터 정본 |
| --- | --- | --- |
| Sense | product plugin + shared context service + local engine | context service |
| Corpus | product plugin + context service + Sync + local engine | context service와 연결된 로컬 파일 |
| Hypes | product plugin + shared context service + local engine | context service |
| Journal | product plugin + service + Site | Journal service D1 |
| Library | product plugin + service + Site | Library service D1·R2 |
| Design | product plugin + service + Site | Design service D1·R2 |
| Document Files | Codex bundled Skills + ChatGPT Personal Skills + host runtime + Sync embedded engine + Claude local MCP | 호출자가 소유한 문서 바이트 |

`services/remote-context`가 Sense·Corpus·Hypes를 함께 호스팅하고 OpenAI용 여섯 제품 도구를 한
표면에 등록하는 것은 배포 단위의 선택이다. 각 제품의 계약, 저장 접근과 업무 로직은 모듈 경계를
유지하며 한 제품의 변경이 다른 제품의 공개 표면을 암묵적으로 바꾸지 않게 한다.

## 기준 구조

상태가 있는 원격 제품은 다음 흐름을 기준으로 한다.

```text
OpenAI 통합 plugin ── registered app ── 통합 MCP ── product module ──┐
Claude 제품 plugin ─────────────────── 제품 MCP ── product module ──┼── D1 / R2
Site ───────────────────────────────────── HTTP ── product service ──┘
```

- **plugin**: manifest, Skill, 아이콘과 연결 정보만 배포한다. OpenAI에서는
  `plugins/personal-agent-toolkit`이 Sense, Corpus, Hypes, Journal, Library, Design과 문서 Skill 및
  host 실행 번들을 Codex의 설치 단위 하나로 제공한다. 비공개 개인 ChatGPT에는 같은 등록 app과
  정본에서 만든 Personal Skills를 설치한다. `Personal Agent Toolkit`은 app·plugin 설치 항목의
  이름이며 제품, Skill과 도구는 짧은 이름을 유지한다. Claude에서는 제품별 plugin을 사용한다.
- **service**: 인증, 입력 검증, 업무 규칙, 동시성 제어와 저장을 소유한다.
- **Site**: 소유자 전용 읽기·편집 UI를 제공하고 제품 service API를 사용한다. Site 자체에 제품
  데이터 binding을 두지 않는다.
- **공통 runtime**: [`packages/remote-runtime`](./packages/remote-runtime)이 owner 인증 계약,
  bearer 처리와 MCP 성공·오류 응답처럼 둘 이상의 제품에서 같은 의미로 반복되는 코드만 제공한다.
- **공통 contract**: Zod schema에서 입력 타입을 추론하고 MCP와 HTTP가 같은 업무 입력을 사용한다.
- **MCP 출력 contract**: 모든 공개 도구는 객체 루트 `outputSchema`를 제공하고 이에 맞는
  `structuredContent`와 JSON text fallback을 함께 반환한다. schema는 클라이언트가 사용할 주요
  필드만 고정하고 제품 내부 응답 전체를 불필요하게 폐쇄하지 않는다.

Design은 Library와 같은 서비스 소유 구조를 쓴다. 개인 레시피와 템플릿 메타데이터는 D1, 파일은
R2에 두고 Site와 MCP가 같은 `services/design`을 사용한다. 공개 저장소, plugin 묶음과 Site source에
개인 자산 사본을 넣지 않는다.

Document Files의 분석 계약은 실행 위치와 분리한다. `AnalysisJob v1`과 byte stream을 Sync나 로컬
Codex의 로컬 package 또는 OpenAI 통합 plugin·개인 ChatGPT Skill의 host runtime에 전달하고 같은
`AnalysisResult v1`을 받는다. 호스트에 필요한 기능이 없으면 `runtime_unavailable`로 중단하며 자동
원격 폴백은 없다.
원문 바이트는 Personal Agent Toolkit 서비스나 Corpus 원격 저장층으로 보내지 않고 Sync는 로컬에서
완료된 projection만 전송한다. Claude는 로컬 파일 권한을 위해 독립 Document Files local MCP를 쓴다.
LibreOffice, macOS PDFKit/Vision과 Office 앱 제어는 실행 경계에 포함하지 않는다.

## 데이터와 변경

서비스가 있는 제품의 D1·R2 binding은 서비스에 둔다. Site에는 제품 상태 저장 binding을 두지
않고 인증된 service 요청만 보낸다. 쓰기 API는 현재 version을 대조해 충돌을 명시적으로 반환하며
마지막 요청이 앞선 편집을 조용히 덮어쓰지 않게 한다.

D1 schema는 versioned migration 파일로만 변경한다. 요청을 처리하면서 `CREATE`나 `ALTER`를
실행하지 않는다. 운영 이전은 기존 데이터 export, 새 저장층 import, 레코드·asset 대조, 전환과
rollback 가능 기간까지만 다룬다. 별도 영구 이력 저장소를 만들지 않고 Git의 migration 기록과
D1의 복구 기능을 사용한다.

Library와 Design은 service가 D1·R2와 version 충돌 검사를 소유하고 Site와 MCP가 같은 service를
사용한다. Site용 내부 token은 사용자 OAuth와 분리한다. 개인 Design 자산의 현재 정본은
`services/design`의 운영 D1·R2이며 저장소의 예전 복사본은 현재 tree와 배포 묶음에서 제거한다.
기존 공개 Git 이력의 정리는 별도의 명시적 결정으로 다룬다.

## 인증과 권한

원격 제품은 공통 소유자 인증에서 principal을 만들고 제품별 scope로 읽기와 쓰기를 제한한다.
Site는 확인된 사용자 ID와 이메일을 모두 요구한다. 내부 Site-to-service token은 사용자 OAuth와
구분하고 저장소에 넣지 않는다. 서비스 오류는 안정적인 code와 안전한 message만 공개하며 내부
예외와 자격 증명을 응답에 포함하지 않는다.

통합 toolkit 리소스의 기존 설치 grant는 제품 추가만으로 끊지 않는다. 새 grant는 Design scope를
명시적으로 사용하고, Design 추가 전에 모든 toolkit 읽기 또는 쓰기 scope를 받은 기존 소유자
grant는 `/mcp` 안에서만 같은 수준의 Design 권한으로 이어받는다. 제품별 Design 리소스에는 이
호환 규칙을 적용하지 않는다.

Sync 장치 인증은 제품 MCP OAuth와 분리한다. Sync endpoint는 projection과 원격 Work 요청만
중계하며 원문 문서 바이트나 문서 분석 요청을 받지 않는다.

## 버전과 배포

plugin base version은 `products.json`과 client manifest에 같이 반영한다. Codex manifest의 packaging
revision만 바꾸는 경우에는 base version과 service version을 올리지 않는다. 같은 제품으로 배포하는
service와 Site package는 같은 release version을 사용한다. `engines/`의 개발·이관 package는 공개
plugin과 독립적으로 바뀐다.

OpenAI 통합 plugin의 base version은 제품별 release version과 별도로 `products.json`의 OpenAI 배포
항목에서 관리한다. 제품 Skill이나 통합 app 구성이 바뀌면 packaging revision을 갱신하며 제품 계약이
함께 바뀐 경우에만 해당 제품의 base version도 올린다.

base version을 바꾼 변경은 소스와 원격 배포를 반영하고 지원 클라이언트의 새 세션에서 현재 Skill,
도구 이름과 입력 구조가 보이는지 확인해야 완료다. 문구 정리처럼 공개 동작을 바꾸지 않는 변경은
불필요하게 version을 올리지 않는다.

OpenAI 통합 plugin은 `.app.json`에 등록 app 하나를 두고 Codex manifest가 이를 참조한다. 통합 MCP는
여섯 상태형 제품의 공개 도구를 제품 모듈에서 직접 등록하고 각 제품의 데이터 저장소와 권한을 그대로
쓴다. 등록 app은 원격 제품 Skill의 필수 의존성으로 선언해 app이 없는 대화에서 원격 도구를 사용할 수
있는 것처럼 노출하지 않는다. Codex 문서 기능은 같은 설치 항목에 bundled Skill과 host runtime으로 포함한다. 개인 ChatGPT의
비공개 등록 app에는 plugin 파일을 결합할 수 없으므로 같은 문서 Skill을 계정의 Personal Skills에
올리되, 생성 archive를 정본으로 보관하지 않는다. 직접 `mcpServers`를 함께 선언하거나 제품별 app과
Document Files Codex plugin을 별도 설치하지 않는다. Claude의 제품별 `.mcp.json`은 각 원격 endpoint
또는 Document Files local MCP 연결을 소유한다.

## 검증 원칙

저장소 검사는 `products.json`과 manifest·package version·실제 공개 도구가 어긋나는 경우처럼
사용자에게 다른 제품이 노출될 수 있는 드리프트만 막는다. 업무 규칙, 권한, migration, byte identity와
손실 위험이 있는 경계는 테스트하되 구현 상수를 그대로 되읽거나 디렉터리 존재를 여러 층에서
반복 확인하는 회귀 테스트는 만들지 않는다. 중간 조사 기록과 생성된 보고서는 저장소에 누적하지
않는다.

## 장기 개선 계획

2026-09-05 전면 검토를 출발점으로 삼는다. 이 절은 작업 범위와 조사·결정 순서를 정하는 계획이며,
위의 현행 설계를 대체하지 않는다. 제품 통폐합, 지원 클라이언트 축소, 지침 개정과 데이터 정책
변경은 아직 채택한 결론이 아니다. 각 범위의 상세 조사 뒤 필요한 변경만 선택한다.

### 목표와 판단 기준

목표는 사용자가 자료를 다시 설명하고, 잘못된 결과를 고치고, 도구를 관리하는 부담을 줄이는 것이다.
기능 수나 코드 감소만으로 성과를 판단하지 않는다. 생산성 향상과 에이전트 기술을 탐구하는 가치는
구분하고, 탐구를 위한 기능에는 별도의 목적과 투자 한도를 둔다.

대표 작업과 초기 투자 순서는 00 범위에서 정한다. 원자료 변경 대응과 안전한 저장·복구는
여러 대표 작업에 공통으로 필요한 조건으로 다루며, 제품마다 별도의 성과 목표를 만들지 않는다.

같은 승인 지침과 자료 접근 범위 아래에서 호스트 기본 기능, 필요한 Toolkit 기능만 사용한 구성,
현재 구성을 비교한다. 재설명과 수작업 횟수, 결과 수정량, 완료 시간, 이후 재사용 여부를 필요한 만큼
확인한다. 유지보수 시간, 운영 비용과 장애 복구 부담도 함께 살핀다. 별도 평가 플랫폼이나 상시 계측
기능을 먼저 만들지 않으며, 조사 결과로 각 범위의 실용적인 완료 기준을 정한다.

### 범위 구성

총괄 하나와 전문 범위 여덟 개를 둔다. 번호는 작업을 나눌 때 사용하는 식별자이며 착수 순서가 아니다.
각 범위는 독립적으로 조사하고 세부계획을 만들 수 있지만, 공유 계약을 바꾸는 구현은 관련 범위와
합의한 뒤 반영한다.

| 범위 | 주제 | 맡는 결정 |
| --- | --- | --- |
| 00 | 전체 가치와 우선순위 | 어떤 작업을 더 잘 지원하고 어디에 투자할지 |
| 01 | Sense와 Hypes | 지침과 사용자 모델을 언제, 얼마나, 어떻게 적용할지 |
| 02 | Corpus 검색과 프로젝트 맥락 | 필요한 자료와 현재 판단을 정확하고 적은 부담으로 찾는 방법 |
| 03 | Sync와 자료 생명주기 | 로컬 접근, 갱신, 보존과 Work 변경의 안전한 작동 |
| 04 | Document Files와 문서 Skill | 직접 유지할 문서 기능과 호스트 기능에 맡길 범위 |
| 05 | Journal | 일상 작업의 정리·이월·마감과 결과 반영 방식 |
| 06 | Library | 읽기·편집·발행·재사용 경험과 운영 부담 |
| 07 | Design | 개인 자산의 발견·선택·적용·관리 방식 |
| 08 | 공통 운영과 배포 | 인증, 공통 계약, 복구와 클라이언트별 배포의 유지 가능성 |

#### 00. 전체 가치와 우선순위

총괄은 대표 작업, 생산성 목적과 실험 목적, 투자 순서와 전체 완료 판단을 맡는다. 이 문서와
[`products.json`](./products.json)을 기준으로 전문 범위의 결과를 종합하며 제품별 상세 설계나 코드를
대신 결정하지 않는다. 아래는 현재 활용 경로와 전면 검토를 바탕으로 정한 초기 우선안이다.
사용 빈도·시간 절감·만족도를 측정한 결과나 제품 지원 정책의 변경으로 해석하지 않는다.

##### 대표 작업

| 대표 작업 | 시작과 결과 | 개선 판단 | 주요 범위 |
| --- | --- | --- | --- |
| 연구·개발 작업의 재개와 진전 | 이전 작업의 목적에서 시작해 현재 자료, 채택된 판단과 열린 질문을 파악하고 다음 검토나 원고 작성까지 진행 | 사용자가 맥락을 다시 설명하는 부담이 줄고, 오래된 판단을 현재 사실로 오인하지 않으며 실제 작업이 진전되는가 | 01·02·03 |
| 한국어 실무 문서의 완성 | 연결된 원자료나 기존 파일에서 시작해 필요한 내용을 읽고 문서·표·발표자료를 작성·수정하여 사용할 수 있는 파일로 마침 | 내용·값·구조의 오류와 사용자의 재작업이 줄고, 요청한 표현과 형식이 유지되는가 | 02·03·04 |
| 축적한 글과 자료의 재사용 | 기존 발간호나 정리한 자료에서 필요한 내용을 찾아 새로운 질문·글·발표 등 구체적인 결과에 활용 | 다시 찾기와 편집이 쉬워지고, 축적한 내용이 이후 작업에 실제로 쓰이는가 | 02·06·07 |

첫 비교는 연구·개발 작업의 재개에서 시작한다. 한국어 실무 문서는 다음 비교 대상으로 두고,
축적한 글의 재사용은 실제 후속 활용이 있는 사례에서 확인한다. Journal의 진행 정리와 원자료
갱신은 이 작업들을 돕는 기능으로 살핀다. 기록·발행·자산 수가 많다는 이유로 활용 효과가 크다고
판단하지 않는다.

##### 비교 방식과 채택 기준

- **비교 조건:** 같은 목적·자료 시점·승인 지침·접근 권한·결과물 기준을 사용한다. 가능하면 같은
  모델 조건에서 호스트 기본 기능, 필요한 Toolkit 기능만 사용한 구성, 현재 구성을 비교한다.
  호스트에 자료와 지침을 다시 전달하거나 찾아야 하는 부담도 포함한다. 준비가 끝난 상태끼리만
  비교해 맥락 복원의 가치를 빠뜨리지 않는다.
- **사례 선택:** 실제 대표 작업 하나로 시작하고, 그 변경의 판단을 바꿀 가능성이 있는 긴 문서,
  표나 변경된 자료 같은 조건만 추가한다. 특정 클라이언트의 읽기 성공을 다른 환경의 동작이나
  전체 작업의 성공으로 확대하지 않는다. 운영 자료에 비교용 변경을 임의로 저장하지 않는다.
- **유지할 조건:** 필요한 사실·값·출처와 자료 시점, 요청한 표현·형식, 승인된 지침, 권한과 변경
  안전성을 유지한다. 읽기나 호출 횟수만 줄어도 결과 품질이나 사용자 통제가 나빠지면 채택하지 않는다.
- **채택할 개선:** 대표 작업에서 재설명·자료 탐색·결과 수정·도구 관리 중 구체적인 부담을 줄이거나,
  기존에 완수하지 못하던 작업을 가능하게 해야 한다. 그 이익과 추가 유지 비용을 비교한다.
  응답 문자 수는 전달량의 지표일 뿐 시간·토큰 비용·생산성 향상의 측정값으로 환산하지 않는다.
- **보류할 개선:** 관리할 설정·데이터 사본·배포 단계만 늘고 효과가 확인되지 않으면 보류한다.
  사용 가치와 관계없이 탐구할 기능은 실험 목적, 끝낼 조건과 감당할 유지 범위를 먼저 정한다.

##### 투자 우선안

| 대상 | 초기 방향 | 이유와 다음 판단 |
| --- | --- | --- |
| Corpus 검색·읽기 | 첫 상세 조사 | 여러 대표 작업의 공통 경로이며 읽기 응답 문제를 저장층 이전 없이 분리해 조사할 수 있음 |
| Document Files와 문서 Skill | 다음 주요 조사 | 실제 파일 완성에 직접 연결됨. HWP/HWPX·복잡한 구조의 가치와 일반 문서 작업의 중복을 사례로 구분 |
| Sync와 자료 생명주기 | 안전성 유지와 확인된 병목 개선 | 자료 갱신·접근·복구를 보장하는 기반. 필요 없이 동기화 구조나 보존 정책을 다시 설계하지 않음 |
| Sense | 적용 경로와 읽기 부담 개선 | 승인 지침의 효력은 유지. 지침 내용의 개정과 Toolkit의 지침 전달 방식은 분리해 결정 |
| Hypes | 검색·수정 안전성부터 검토 | 알려진 작동 문제와 관계 모델의 효용을 구분. 그래프 확장은 실제 판단 개선을 확인한 뒤 선택 |
| Journal·Library | 현행 사용을 유지하며 구체적인 불편 개선 | 진행 정리·발행·편집 경로는 존재함. 사용자가 관리해야 하는 일과 실제 재사용 효과를 조사한 뒤 확대 |
| Design | 자산을 찾고 적용하는 흐름부터 확인 | 빈 후보 상태의 원인과 준비 조건을 먼저 해결할 대상으로 검토. 자산 수나 선택 분류의 확대는 뒤로 둠 |
| 공통 운영·배포 | 필요한 안전 조건과 release 지원 | 변경마다 필요한 권한·복구·실제 클라이언트 확인을 수행. 서비스 통합과 지원 대상 축소는 비용 조사 뒤 별도 결정 |

실제 착수 순서는 첫 Corpus 조사 뒤 다시 정한다. Hypes 검색이나 Design 후보 표시처럼 독립적인
작동 문제는 작은 별도 변경으로 앞당길 수 있다. 안전성 문제가 확인되면 생산성 순위보다 먼저
처리한다. 지원 클라이언트는 현행을 유지하고 08에서 역할과 비용을 조사한다. 초기 읽기 조사를
현재 Codex 환경에서 수행하는 것은 다른 클라이언트의 지원을 축소하는 결정이 아니다.

##### 첫 적용 범위: Corpus Source 읽기

02의 첫 조사에서 본문 페이지와 전체 unit이 함께 반복되는 응답, 작동하지 않는 구조 맥락 옵션과
누락된 revision 수집 시각을 확인했다. 같은 도구에 선택형 `text` 응답을 추가하고 생략 시 기존
`full`을 유지하는 세부계획과 표적 회귀 범위를 채택했다.

본문·출처·구조, 페이지·크기 제한과 배포·복구 계약은
[Corpus 설계의 Source 읽기 응답](./plugins/corpus/DESIGN.md#source-읽기-응답)에 둔다.
검색 알고리즘, Context 자동 수정, 파서·Sync·보존 정책과 저장층 변경은 이 구현에 포함하지 않는다.
구현·표적 회귀와 실제 Source 읽기 비교는 확인했다. 남은 클라이언트 적용 확인은 다음 공통
원격 서비스 반영 묶음에 포함하며, 그 대기 때문에 독립적인 다음 조사를 멈추지 않는다.

#### 01. Sense와 Hypes

- **범위:** 지침의 탐색·선택·전달, Section Skill 연결, 사용자 모델의 검색·적용·수정 안전성을
  다룬다. 승인된 지침 본문이나 개인 관계 데이터를 일괄 축약·재작성하는 작업은 포함하지 않는다.
- **조사 질문:** 매 작업에서 읽는 내용이 실제 판단을 얼마나 바꾸는가? 같은 목적의 지침이
  중복되거나 충돌하는가? Hypes의 관계 구조가 더 단순한 사용자 모델보다 유용한가? 한국어 검색과
  여러 클라이언트의 동시 수정이 의도대로 작동하는가?
- **초기 확인 항목:** 원격 Hypes의 한국어 `focus` 검색 누락을 재현하고, 쓰기 버전 대조의 부재가
  현행 충돌 방지 원칙과 어떻게 어긋나는지 확인한다. 검색 수정과 사용자 모델 자체의 개편은 별도
  변경으로 계획한다.
- **결정 결과:** 반드시 읽을 지침과 필요할 때 읽을 지침의 구분, 사용자 모델의 적용 조건,
  검색·수정 계약의 세부계획을 만든다. 승인 지침을 빠뜨리지 않으면서 불필요한 읽기를 줄이고,
  필요한 사용자 관계를 찾으며 오래된 편집이 조용히 덮어쓰지 않는 것을 완료 기준으로 삼는다.
- **정본과 연결:** [`plugins/sense/DESIGN.md`](./plugins/sense/DESIGN.md),
  [`plugins/hypes/DESIGN.md`](./plugins/hypes/DESIGN.md)와 각 제품 Skill을 먼저 읽는다.
  구현은 `services/remote-context/src/sense.ts`, `hypes.ts`에서 확인한다. 공통 변경·응답 계약은
  08과 맞추며, 로컬 개발·이관 engine의 정리 필요성은 실제 사용 여부를 확인한 뒤 판단한다.

#### 02. Corpus 검색과 프로젝트 맥락

- **범위:** Space·Context 탐색, Source 검색과 읽기, 출처·시점·구조의 전달, Context 수정 흐름을
  다룬다. 파일 감시·로컬 쓰기는 03, 문서 추출기의 정확성은 04가 맡는다.
- **조사 질문:** 프로젝트 재개에 필요한 자료와 채택된 판단을 얼마나 빨리 찾는가? 검색 실패는
  자료 부족, 질의 처리, 추출 오류 중 어디에서 생기는가? 응답 본문과 구조 정보가 중복되는가?
  원자료 변경이 기존 판단에 영향을 주는지 사용자가 어떻게 검토할 수 있는가?
- **초기 확인 항목:** Source 읽기에서 `max_chars`로 줄인 본문과 별개로 전체 `units`가 반환되는
  경로를 확인한다. 응답 크기 제한의 의미와 상세 구조의 제공 방식을 정하되 출처와 재현 근거를
  저장층에서 제거하는 문제로 확대하지 않는다.
- **결정 결과:** 대표 작업의 검색·읽기 흐름, 기본 응답과 상세 조회의 역할, 변경된 원자료와
  기존 Context를 함께 검토하는 방식의 세부계획을 만든다. 검색 방식 확장은 확인된 실패 원인으로
  정한다. 필요한 근거를 찾고 현재 자료와 과거 판단을 구별할 수 있어야 한다.
- **정본과 연결:** [`plugins/corpus/DESIGN.md`](./plugins/corpus/DESIGN.md)의 Source 검색·Context·MCP
  절과 [`services/remote-context/DESIGN.md`](./services/remote-context/DESIGN.md)를 먼저 읽는다.
  `services/remote-context/src/corpus.ts`, `corpus-shard.ts`를 확인한다. 03과 자료 정체성·상태의 의미를,
  04와 추출 결과의 경계를, 05와 확정 결과의 Context 반영 방식을 맞춘다.
- **첫 적용:** Corpus 설계 문서의 「Source 읽기 응답」에서 선택형 본문 읽기와 기존 상세 응답,
  페이지·출처·구조 맥락의 계약을 관리한다. Source 읽기 개선을 Corpus 전체 검색·저장 구조의
  개편으로 확대하지 않는다.

#### 03. Sync와 자료 생명주기

- **범위:** 로컬 연결과 권한, 파일 정체성, 변경 감지와 갱신, 원격 작업 전달, Source record 보존,
  Work 읽기·교체·삭제·복구를 다룬다. 검색의 사용 경험과 문서 파서 내부는 각각 02와 04에 둔다.
- **조사 질문:** 이동·오프라인·추출 실패 뒤 어떤 상태로 복구되는가? 현재 파일과 보존된 record를
  혼동하지 않는가? 반복 갱신과 정리의 비용은 적절한가? 파일 삭제, 등록 해제와 record 삭제의 차이를
  사용자가 이해하고 통제할 수 있는가? 원격 요청에도 로컬 권한과 최신 버전 검사가 유지되는가?
- **결정 결과:** 실제 정상·실패 흐름과 복구 가능 범위를 확인한 뒤 갱신·보존·Work 변경의 개선안을
  정한다. 기존 안전장치를 유지하면서 갱신 상태를 설명할 수 있고, 오래된 요청과 경로 변경이 잘못된
  파일 변경으로 이어지지 않는 것을 완료 기준으로 삼는다.
- **정본과 연결:** [`apps/sync/README.md`](./apps/sync/README.md),
  [`plugins/corpus/DESIGN.md`](./plugins/corpus/DESIGN.md)의 정체성·갱신·보존·Work 절,
  [`services/remote-context/DESIGN.md`](./services/remote-context/DESIGN.md)를 먼저 읽는다.
  `apps/sync`와 `engines/corpus`가 로컬 실행 조사 범위다. 02와 상태 표현을, 04와 분석 요청·결과를,
  08과 인증·운영 복구를 맞춘다. 보존 기간 변경과 운영 데이터 정리는 별도 결정으로 다룬다.

#### 04. Document Files와 문서 Skill

- **범위:** 문서 분석 계약과 형식별 추출, 호스트 실행 번들, `document-files`, `documents`, `pdf`,
  `spreadsheets`, `presentations` Skill의 작업 분담과 유지 범위를 검토한다. Sync 등록·갱신 정책과
  개별 업무 문서의 내용을 대신 바꾸지 않는다.
- **조사 질문:** HWP/HWPX와 복잡한 표 등 직접 유지할 가치가 큰 기능은 무엇인가? 일반 문서 작업은
  호스트 기본 기능과 비교해 어디에서 차이가 나는가? 정확한 추출과 편집 결과의 시각적 완성도를
  각각 어떻게 확인하는가? 지원 환경이 늘어날 때 유지 비용은 어디에 집중되는가?
- **결정 결과:** 실제 문서 사례를 바탕으로 유지·보강·위임할 기능, 분석과 제작 Skill의 역할,
  필요한 호스트 기능과 실패 처리의 세부계획을 만든다. 추출은 본문·구조·값을, 제작은 사용 가능한
  파일과 표현을 기준으로 판단한다. 대규모 파서 재작성은 확인된 결함과 가치가 있을 때만 선택한다.
- **정본과 연결:** [`plugins/document-files/DESIGN.md`](./plugins/document-files/DESIGN.md), 같은 제품의
  `skills`와 Python package를 시작점으로 삼는다. `AnalysisJob v1`·`AnalysisResult v1`의 변경은 02·03과,
  host 번들과 client 배포 변경은 08과 맞춘다. 생성된 통합 plugin이나 설치본을 정본처럼 고치지 않는다.

#### 05. Journal

- **범위:** 기록의 수집·정리, 상태와 책임의 표현, 일상 조회, 이월·주간 마감, 확정 결과의 Corpus
  반영을 다룬다. 실제 항목의 완료·취소·마감이나 자동화 실행은 제품 조사와 구분한다.
- **조사 질문:** 사용자가 정리해야 할 일을 줄이는가, 별도의 관리 일을 만드는가? 보류 항목의 이월
  의미는 적절한가? 주간 마감과 Corpus 반영을 결합한 현재 방식이 필요한가? 반복 수집과 정정이
  중복·오해 없이 처리되는가?
- **결정 결과:** 실제 사용 흐름에 맞춘 상태·이월·마감과 결과 반영의 세부계획을 만든다. 관리
  부담을 줄이면서 명시적 완료 판단, 과거 기록과 현재 상태의 차이를 보존해야 한다. 현행 유지나
  일부 기능 축소도 선택지로 둔다.
- **정본과 연결:** [`plugins/journal/DESIGN.md`](./plugins/journal/DESIGN.md),
  [`services/journal/DESIGN.md`](./services/journal/DESIGN.md), 제품 Skill과 `sites/journal`을 읽는다.
  Corpus 반영의 의미는 02와 합의하고, 상태 변경을 동반한 운영 자료 이전은 08의 복구 조건을 따른다.

#### 06. Library

- **범위:** 목록과 읽기 흐름, 직접 편집, 발행과 자산 관리, 기존 글의 탐색·재사용, 운영 구조를
  다룬다. 발행 일정이나 편집 방향의 변경, 기존 호의 내용 개작은 별도 요청 범위로 남긴다.
- **조사 질문:** 발행한 글을 다시 읽고 활용하기 쉬운가? 본문·표지·삽화를 고치는 데 어떤 불편이
  있는가? 발행 자동화와 읽기·편집 경험은 어떻게 연결되는가? 정본 문서의 이전 설명과 실제 저장·배포
  상태가 일치하는가?
- **결정 결과:** 읽기·수정·발행·재사용에서 개선할 흐름과 유지할 편집 경험을 정한다. 발행량보다
  실제 열람·수정·재사용과 운영 부담으로 판단한다. 서비스·자산 정본과 충돌 보호가 실제 동작과
  일치해야 완료로 본다.
- **정본과 연결:** [`plugins/library/DESIGN.md`](./plugins/library/DESIGN.md),
  [`services/library/README.md`](./services/library/README.md), `sites/library`와 제품 Skill을 읽는다.
  편집 지침이 필요한 조사에는 해당 Corpus Context를 읽되 이 Toolkit 계획을 편집 맥락에 저장하지
  않는다. 공통 자산 사용은 07, 인증·배포·복구는 08과 맞춘다.

#### 07. Design

- **범위:** 개인 레시피·템플릿·자산의 등록, 준비 상태, 검색·미리보기·선택·적용과 관리 경험을
  다룬다. 새 레시피의 대량 제작이나 개인 자산의 공개 배포는 포함하지 않는다.
- **조사 질문:** 사용자가 만들려는 결과에서 적절한 자산을 찾고 적용할 수 있는가? 준비 상태와
  검증 상태가 실제 사용 가능성을 나타내는가? 선택 절차가 도움이 되는가? 보유한 자산을 개선하는
  비용과 매번 새로 만드는 비용은 어떻게 다른가?
- **초기 확인 항목:** 레시피가 존재해도 `selection_ready` 조건으로 Site 후보가 비는 흐름을 확인한다.
  상태값을 일괄 변경하기 전에 준비 조건, 미준비 자산의 표시와 사용자 안내를 함께 결정한다.
- **결정 결과:** 자산을 발견하고 실제 산출물에 적용하는 흐름, 준비 상태와 검증의 의미, 관리 방식의
  세부계획을 만든다. 필요한 예시와 사용 가능한 자산을 찾을 수 있고, 사용할 수 없을 때 이유와 다음
  행동을 알 수 있어야 한다.
- **정본과 연결:** [`plugins/design/DESIGN.md`](./plugins/design/DESIGN.md),
  [`services/design/README.md`](./services/design/README.md), `sites/design/app/design-gallery.tsx`와 제품
  Skill을 읽는다. 개인 자산은 service에서 필요한 만큼만 확인한다. 06과 재사용 요구를, 08과 권한·배포를
  맞추며 공개 저장소에 자산 사본을 만들지 않는다.

#### 08. 공통 운영과 배포

- **범위:** 소유자 인증과 권한, 제품 공통 변경·오류·응답 계약, 저장층의 운영 복구, 배포 단위와
  클라이언트별 설치·갱신, 공통 코드와 실제 운영 비용을 다룬다. 제품별 업무 규칙과 지원 대상의
  존폐를 이 범위에서 단독 결정하지 않는다.
- **조사 질문:** 새 제품 권한의 동의와 기존 grant 호환은 어떻게 다뤄야 하는가? 추출 텍스트와
  사용자 데이터의 노출·보존·복구 경계가 명확한가? 통합 MCP와 개별 서비스의 버전이 달라질 수
  있는가? 배포 단위를 유지하거나 합칠 때 얻는 이익과 결합 비용은 무엇인가? 실제로 쓰는 환경을
  갱신하고 확인하는 데 얼마나 드는가?
- **결정 결과:** 먼저 공통 안전 조건과 복구 가능 범위를 확인한다. 이후 제품별 요구를 받아
  유지할 공통 계약, 배포·지원 방침과 단순화할 구현의 세부계획을 만든다. 이전 전 export·복원 확인,
  변경 실패 시 복구 방법, 지원 클라이언트의 실제 동작 확인이 가능해야 한다. 문서상 설치 지원과
  현재 세션의 동작 확인은 구분한다.
- **정본과 연결:** 이 문서, [`products.json`](./products.json), [`PRIVACY.md`](./PRIVACY.md),
  [`auth/README.md`](./auth/README.md), `packages/remote-runtime`, `packages/site-runtime`,
  `services/remote-context/src/mcp.ts`, `schemas.ts`와 배포 스크립트를 읽는다. 지원 정책은 00과,
  각 계약·저장 변경은 해당 제품 범위와 결정한다. 전체 서비스 통합이나 공통 추상화를 먼저 구현하지 않는다.

### 착수 순서와 의존 관계

1. **기준 설정과 초기 문제 확인:** 00에서 대표 작업과 우선순위를 정하고 08에서 변경 전 필요한
   권한·데이터·복구 조건을 확인한다. 01의 한국어 검색, 02의 응답 크기, 07의 빈 후보 화면은 앞선
   검토의 재현 후보로 삼는다. 2026-09-05 검토 시점의 소스는 `95d0f0b`이며, 착수할 때 현재 코드와
   운영 상태를 다시 확인한다. 기존 계약 안의 작은 수정은 전체 조사 완료를 기다리지 않아도 된다.
2. **자료 활용의 주요 흐름 개선:** 02·03·04에서 검색·읽기, 갱신·Work 접근, 문서 처리의 경계를
   맞추고 가치가 확인된 변경부터 진행한다. 세 범위를 하나의 대규모 재작성으로 묶지 않는다.
3. **제품별 사용 경험 개선:** 01의 지침 적용, 05의 일상 관리, 06의 읽기·편집, 07의 자산 활용을
   실제 불편과 이용 가치에 따라 선택한다. 앞 단계 전체가 끝나야 조사나 독립적인 수정을 시작할 수
   있는 것은 아니다.
4. **공통 구조 정리와 통합 확인:** 08은 선택된 제품 변경을 배포에 반영하면서 실제로 중복되는
   코드와 운영 단위만 정리한다. 00에서 대표 작업을 다시 수행해 개선 효과와 남은 유지 비용을
   판단하고 다음 범위의 착수·보류·종료를 정한다.

동시에 구현하는 범위는 우선 한두 개로 제한하고, 겹치지 않는 조사는 별도 맥락에서 진행할 수 있다.
전체 일정과 범위별 공수는 상세 조사 이후에 정한다. 모든 범위의 세부계획을 먼저 완성하거나,
모든 제품을 반드시 변경하는 것을 이 계획의 완료 조건으로 삼지 않는다.

### 범위별 진행 방식

각 범위는 **상세 조사·검토 → 세부계획 → 구현 → 실제 사용 확인** 순서로 진행한다.

1. **상세 조사·검토:** 해당 범위의 정본과 실제 구현·운영을 읽고 대표 흐름을 확인한다. 현행 유지,
   최소 수정, 구조 변경이나 호스트 기능 활용 중 현실적인 대안을 비교한다. 앞선 검토에서 확인한
   현상, 코드에서 추정한 위험과 새 제안을 구별한다. 운영 데이터 변경이 필요한 확인은 읽기 조사와
   분리한다. 결정에 충분한 근거를 얻으면 조사를 마친다.
2. **세부계획:** 해결할 문제와 범위 밖의 일, 선택안과 이유, 바뀌는 계약·정본·코드, 선행 결정,
   구현 순서와 완료 기준을 정한다. 데이터 이전·권한·배포에 영향이 있으면 복구 방법과 실제 확인
   대상까지 포함한다. 현재 문제에 필요한 기존 검사와 추가 검사가 필요한 이유를 구분한다.
3. **구현:** 이 전체 개선 목표와 기존 계약 안에서 조사 결과를 세부계획으로 정리하고 구현까지
   자율적으로 진행한다. 매 단계마다 사용자에게 다음 단계의 시작을 요청하지 않는다. 제품 구성이나
   데이터 정책처럼 사용자의 선택이 필요한 변경은 아래 질문 기준을 따른다.
4. **실제 사용 확인:** 해당 대표 작업과 변경에 필요한 검사를 수행한다. 구현·배포·클라이언트 노출을
   구분하고, 적용되는 release 절차까지 확인한다. 완료되면 정본을 현재 동작에 맞추고 다음 작업에
   필요한 결정과 미완료 사항만 남긴다.

각 범위의 조사 결론은 구현뿐 아니라 현행 유지·보류·축소가 될 수 있다. 기대 효과가 유지 비용을
정당화하지 못하거나 필요한 근거를 얻지 못했다면 범위를 줄이거나 보류한다. 사용자 지침, 제품
존폐, 지원 클라이언트와 보존·삭제 정책은 주요 선택으로 함께 검토한다. 현재의 원문
바이트·로컬 권한 경계는 명시적으로 변경하기 전까지 그대로 지킨다.

### 장기 목표의 운영

2026-09-05부터 이 전체 계획을 하나의 지속 목표로 진행한다. 범위별 조사 결론과 채택한 변경을
기존 정본에 반영하며, 전체 목표는 00~08의 판단과 필요한 구현·사용 확인을 마쳤을 때 완료한다.
모든 제품의 개편을 강제하지 않으며, 현행 유지나 보류의 이유와 다시 검토할 조건도 유효한 결론이다.

- **반복 단위:** 한 번에 한두 범위의 독립적인 변경을 다룬다. 조사에서 해결할 문제를 좁히고,
  세부계획·구현·필요한 확인을 마친 뒤 실제 효과와 남은 문제로 다음 순서를 정한다. 기존 검사와
  일회성 확인을 우선하며, 별도 승인이 필요한 새 테스트·검증 자산을 자동으로 늘리지 않는다.
- **사용자 선택:** 제품의 역할·존폐, 지원 대상, 데이터의 소유·보존 등 사용자 구성에 따라 결과가
  달라지는 결정은 해당 변경을 멈추고 쟁점·권고안·선택의 영향을 모아 묻는다. 가능한 환경에서는
  계획 모드와 질문 도구를 사용하고, 그 기능이 없으면 같은 내용을 직접 질문한다. 답을 받기 전에
  그 선택에 의존하는 변경을 실행하지 않는다.
- **작업 승인:** 이 개선 작업에 필요한 설치·권한 설정·확인용 프롬프트 전송은 사용자가 진행을
  허용했다. 단순 실행 승인을 반복해서 묻지 않되, 도구의 필수 안전 확인과 실제 사용자 인증을
  대신하지 않으며 이 요청을 다른 프로젝트나 불필요한 데이터 전송의 승인으로 확대하지 않는다.

### 반영 묶음과 확인 시점

코드의 변경·검사와 여러 플랫폼의 설치·실행 확인은 다른 단위로 관리한다. 같은 원격 서비스나
공개 입력·Skill을 함께 바꾸는 관련 변경은 한 번의 release로 묶는다. 매 작은 변경마다 삭제·설치를
반복하지 않고, 정상적인 업데이트를 먼저 사용한다. 재설치는 이전 버전이 남거나 설치가 손상된
경우에만 선택한다.

각 구현에서는 관련 검사와 현재 환경의 실제 동작을 확인한다. 전체 플랫폼 갱신은 주요 기능 묶음이
완성됐을 때, 호환성이나 특정 플랫폼 동작을 확인해야 할 때, 또는 사용을 막는 결함의 수정이 필요할
때 수행한다. 묶음은 의존 관계와 복구 가능한 크기로 정하며 모든 범위가 끝날 때까지 배포를 미루지
않는다. 기존 클라이언트와 호환되지 않는 서버 변경은 필요한 클라이언트 갱신과 같은 반영 단위로
다룬다.

묶음 확정 시 제품 version·manifest·통합 번들을 맞추고, 변경한 제품과 영향을 받는 클라이언트만
갱신한다. 확인하지 않은 환경은 대기 상태로 남긴다. 구현·원격 반영·클라이언트 적용을 구분하며,
그 묶음의 필수 확인이 끝나기 전에는 release 완료로 보고하지 않는다.

### 맥락 분리와 정본 반영

별도 작업은 공통 지침과 이 계획의 해당 범위, 연결된 정본의 필요한 절부터 읽는다. 전체 검토 대화와
다른 제품 자료를 모두 가져오지 않는다. 작업을 시작하거나 넘길 때 다음 내용만 짧게 전달한다.

```text
범위: 번호와 주제 / 이번 단계는 조사·계획 또는 채택된 계획의 구현
목적과 경계: 이번에 해결할 문제, 제외할 일
읽을 정본: 현재 계약과 관련 문서의 경로·절
채택된 결정: 유지할 조건과 이미 결정한 변경
확인할 사항: 재현할 현상, 미확인 가정과 그 근거 위치
연결 범위: 먼저 맞춰야 할 계약과 함께 바뀌는 파일
다음 행동: 이번 작업의 결과물, 완료 기준, 추가 결정이 필요한 조건
```

전체 구조와 범위 간 합의는 이 문서, 제품별로 채택한 설계와 세부 변경 계획은 해당 제품의 기존
설계 문서에 반영한다. 계획 중인 변경은 현행 계약과 구분하고 구현 뒤 현재 동작으로 정리한다.
`products.json`과 manifest는 실제 공개 표면·배포 정보를 맡으며 계획 후보를 미리 반영하지 않는다.
조사 보고서, 인계 사본과 진행 이력을 별도 문서로 누적하지 않는다.

프로젝트 전반의 진행 상태와 여러 자료를 가로지르는 맥락을 지속해서 저장할 때에는 해당 Corpus
Context를 사용한다. 연결 여부를 먼저 확인하고, 다른 제품의 편집 Context를 대신 사용하지 않는다.
연결을 새로 만들기 전에도 이 계획과 각 작업의 짧은 인계만으로 범위를 나눠 진행할 수 있다.

공유 파일은 통합 변경을 맡는 작업에서 조정한다. 02와 03은 같은 Corpus 설계 문서에서도 맡은 절을
구분하고, 01·02·03·08이 함께 사용하는 MCP 등록·schema는 관련 입력·출력 계약을 합의한 뒤 반영한다.
배포 목록과 통합 plugin 생성은 제품별 변경을 모은 08의 통합 단계에서 처리한다. 남은 클라이언트
확인은 위 반영 묶음에 포함하며, 구현 범위의 완료와 release의 완료를 서로 대신하지 않는다.

첫 적용 대상은 **02의 Corpus Source 읽기 응답**이다. 채택한 계약의 구현·표적 회귀·원격 배포를
마쳤으며, 실제 Source에서 본문과 구조를 보존하면서 응답 크기가 줄어드는 것을 확인했다.
Codex·Claude Code·Claude Desktop의 설치본과 OpenAI 등록 app의 도구 입력도 갱신했다.
Claude Desktop은 Corpus 0.22.0의 현재 Skill 본문을 확인한 뒤 Hypes와 함께 재시작했다.

**01의 Hypes 검색·수정 안전성**은 한국어 질의 처리와 그래프 버전 대조를 구현했다. 읽은 뒤의
변경과 저장 직전의 경합을 모두 거부하며, cascade 결과도 실제로 삭제된 관계와 맞췄다. 기존 검사와
D1의 일회성 충돌·롤백 확인을 마치고 Hypes 0.10.0의 migration과 원격 배포를 적용했다.
운영 관계·FTS를 내보내 복원한 결과와 migration 전후 내용을 대조했으며, 실제 한글 검색과 그래프
버전 반환을 확인했다. 사용자 관계 내용은 바꾸지 않았다. Codex의 현재 통합 Skill, OpenAI 등록
app의 필수 `expected_version`, Claude Code 새 세션의 도구 입력·읽기와 Claude Desktop/web의
0.10.0 plugin을 확인했다. Desktop은 기존 plugin의 업데이트 기능을 사용했으며 재설치하지 않았다.

설치·등록 갱신을 새 작업에서의 실제 사용과 구분한다. 갱신 뒤 새 Codex 검증 작업에서 Hypes의
필수 버전 입력과 한국어 검색 결과를 확인했고, 앱 재시작 후 현재 통합 Skill 목록도 확인했다.
개인 ChatGPT와 새 Cowork 작업의 실제 호출 확인은 남아 있다. 이 상태를 전체 release 완료로
간주하지 않으며, 다음 사용 확인 시점에 이어서 확인한다.

**04의 문서 기능·유지 범위**는 HWP/HWPX의 구조·위치 보존과 공통 분석 계약을 직접 유지하고,
일반 문서 제작은 호스트 라이브러리에 맡긴다. Personal Skill의 실행 명령과 MCP 응답을 복구한 뒤
읽기 경로를 통일하고 셀 전체 사전조건과 값 손실을 막는 편집 제한을 적용했다.
실제 한국어 문서의 읽기·셀 편집과 원본 보존을 확인했으며, 문서 제작 Skill의 원본·편집 가능 요소
보존과 요청된 Google native 결과의 완료 기준도 정리했다. 자체 문서 제작 엔진은 추가하지 않는다.
Document Files 1.5.0과 문서 Skill, Sync 0.2.1을 소스 `0e925e3`과 원격 검사에서 한 묶음으로 확인했다.
Codex 설치본의 소스 일치와 HWP 읽기, Claude Code 새 세션의 local MCP 9개와 실제 읽기를 확인했다.
Claude Desktop은 기존 plugin을 갱신해 새 Skill을 확인하고 재시작했으며, 개인 ChatGPT의 다섯
Personal Skill도 기존 등록을 교체한 뒤 새 본문과 Document Files의 실행 파일 목록을 확인했다.
Codex의 중복 기본 문서 Skill 네 개만 제외했고, 기반 plugin·호스트 기능·별도 Excel 제어와 템플릿
Skill은 유지했다. 현재 앱에서 자동 전달되는 Skill 안내는 재시작 후 확인이 남아 있다. 개인 ChatGPT와
Cowork에서 새 문서 도구를 실제로 사용하는 확인도 남아 있으며, 설치 갱신만으로 대신하지 않는다.
세부 범위와 남은 실제 문서 확인은 [Document Files 설계](plugins/document-files/DESIGN.md)에 둔다.

**03의 Source 이동·교체 안전성**은 이동과 내용 변경이 겹칠 때의 갱신 누락, 추적 파일 사이의
경로 덮기 충돌과 캡처 직전 파일 교체를 재현하고 수정했다. 같은 바이트의 형식 변경은 새 projection과
MIME을 반영하며, 처리 중에 관측한 후속 변경은 대기열에 남긴다. 기존 검사와 일회성 재현 확인 뒤
Sync 0.2.1 실행 환경을 Document Files와 함께 갱신했다. 설치된 소스 일치, 기존 LaunchAgent의 새
프로세스 시작과 연결·대기열 상태를 확인했으며, 등록 설정은 유지했다. 운영 Source 강제 갱신이나
Work 변경을 확인용으로 실행하지 않았다. 이전 record의 보존과 Work 변경 계약도 유지한다.
이미 완료로 기록된 과거 projection을 일괄 재분석하지 않으며, 실제 영향이 확인된 문서만 개별
갱신 대상으로 삼는다. 현재 동작은 [Sync 설명](apps/sync/README.md)에 둔다.

**05의 Journal 첫 조사**에서는 기록 수집과 사용자 처리 상태를 분리한 기본 구조를 유지하고,
Corpus 반영 실패 후 복구와 업무의 주간 연속성을 다음 범위로 좁혔다. 반영 실패 기록이 같은 결과의
재처리도 막는 코드 경로를 확인했으며, 운영에서의 발생 여부는 미확인이다. 주간 마감과 Corpus
반영의 결합 여부, 보류를 다음 주에도 유지할지는 사용자에게 질문한 상태다. 해당 선택에 의존하는
구현과 운영 상태 변경은 하지 않았다. 조사 결론과 다음 계획의 경계는
[Journal 설계](plugins/journal/DESIGN.md)에 둔다.

**01의 지침 전달과 사용자 모델 적용**은 필요한 섹션의 선택 조회와 같은 작업에서의 재사용을
유지한다. 승인 지침을 일괄 축약하거나 별도 캐시·추론 서비스를 추가하지 않는다. Hypes는 설명과
선택을 바꿀 관계에 한해 사용하며 현행 저장 구조를 유지한다. 그래프가 단순 메모보다 우월하다는
비교 근거는 얻지 못했으므로 구조 확대와 일괄 이전 모두 보류한다. 상세 기준은 각 제품 설계에 둔다.
Sense의 일반 전체 조회에 민감 항목이 포함되는 경로와 원격 검토 화면의 구형 항목 표시를 가상
자료로 재현하고 수정했다. 일반 읽기의 제외와 명시적 항목 읽기·로컬 전체 읽기 보존을 확인했으며,
승인 지침과 운영 프로필은 바꾸지 않았다. Sense 0.3.7은 아래 Design 묶음과 함께 운영에 반영했다.

**06의 Library 편집 안전성**은 읽기·발행·직접 편집을 유지하면서 충돌 초안 복구와 이미지 경로
보호를 구현했다. 복원한 초안은 검토 후 저장하며, 제안 취소와 늦은 응답이 사용자의 후속 수정을
지우거나 승인하지 않게 했다. 격리된 상태 전이 14개와 D1·R2 확인에서 기존 이미지·문서 참조와
미저장 수정의 보존을 확인했다. 운영 문서를 확인용으로 수정하지 않았으며, Library 0.3.2의 두
서비스·Site와 영향받는 Skill을 한 묶음으로 반영한다. 검색 확대·태그 영구 저장·발행 정책 변경은
별도로 남긴다. 현재 동작과 확인 범위는 [Library 설계](plugins/library/DESIGN.md)에 둔다.

**07의 Design 발견·안내**는 선택 조건을 유지하면서 제외된 레시피도 최소 정보로 찾고, 정확한
ID·version으로 정보 확인 요청을 이어갈 수 있게 구현했다. 가상 자료의 네 가지 렌더링 상태와 기존
추천 조건·WebMCP 응답을 확인했으며 운영 레시피·파일·선택 상태는 바꾸지 않았다. 선택·검증 정책의
개편과 자산 확대는 실제 후속 활용이 확인될 때 다시 검토한다.

**Sense·Design 반영 묶음**은 소스 `34fc9fc`의 원격 검사를 통과한 뒤 Sense 0.3.7과 Design 0.3.1의
두 Worker, Design Site의 비공개 배포를 반영했다. Codex의 연결된 도구에서 Sense 전체 읽기와
새 검토 제목, Design 목록 읽기를 확인했고, 운영 프로필·Section Skill의 version·갱신 시각과
레시피 목록·revision·선택 상태가 반영 전후 동일했다. Design Site의 소유자 전용 공개 범위는
유지했으며 비로그인 요청도 차단됐다. 로그인한 브라우저에서의 조작 확인은 남아 있다.

공개 도구 입력과 OpenAI 통합 Skill은 바뀌지 않아 해당 설치본을 다시 만들지 않았다. Sync는
원격 version 확인값을 포함한 소스로 정상 갱신했고, 설치 소스 일치와 기존 서비스의 새 프로세스·
연결·대기열 상태를 확인했다. Claude Code의 두 plugin은 기존 사용자 설치를 업데이트해 소스
일치를 확인했다. Sense는 중복 연결을 제외한 새 비지속 세션에서 현재 version·도구 네 개·색인
읽기가 확인됐지만, 일반 세션에는 plugin과 claude.ai의 같은 연결을 서로 억제하는 문제가 남았다.
Design은 인증이 필요해 새 CLI 세션의 도구 호출을 확인하지 못했다.

Claude 웹의 Sense 설치 표시는 0.3.6이며 갱신 메뉴가 없었고, Personal Design은 설치 목록에
없었다. Desktop 창 제어도 정상 완료되지 않아 이 환경의 업데이트를 확인하지 못했다. 별도
시크릿 채팅에서는 문서 Skill이 보였으나 local 문서 MCP가 없었고, Hypes 호출은 승인 응답 없이
실행되지 않았다. 이를 새 Cowork 작업의 실제 사용 확인으로 대신하지 않는다. 남은 인증·중복
연결과 Desktop 갱신은 08의 다음 플랫폼 점검에서 함께 다루며 전체 release 완료로 보고하지 않는다.
