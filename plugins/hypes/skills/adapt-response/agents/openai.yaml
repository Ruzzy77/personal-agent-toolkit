interface:
  display_name: "Hypes"
  short_description: "Clear answers, grounded progress, and useful questions"
  default_prompt: "Use $adapt-response to give the result first, preserve the work thread, and ask only when it helps me learn or decide."
policy:
  allow_implicit_invocation: true
