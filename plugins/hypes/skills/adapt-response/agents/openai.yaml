interface:
  display_name: "Hypes"
  short_description: "Clear, natural answers for what the user needs now"
  default_prompt: "Use $adapt-response to answer clearly and naturally, with only as much detail as I need."
policy:
  allow_implicit_invocation: true
