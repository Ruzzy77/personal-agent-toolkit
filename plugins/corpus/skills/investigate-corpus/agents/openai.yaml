interface:
  display_name: "Find in Corpus"
  short_description: "Find exact sources and reuse selected context"
  default_prompt: "Use $investigate-corpus to find current source passages or continue my selected corpus context."
dependencies:
  tools:
    - type: "mcp"
      value: "corpus"
      description: "Local Corpus overview, exact file and provider-task reading, reusable context, and optional general-view tools"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
