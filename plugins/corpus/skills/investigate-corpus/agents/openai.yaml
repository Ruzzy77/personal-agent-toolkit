interface:
  display_name: "Read with Corpus"
  short_description: "Find and read the exact current sources"
  default_prompt: "Use $investigate-corpus to find the relevant Corpus context and read the exact current sources before answering."
dependencies:
  tools:
    - type: "mcp"
      value: "corpus"
      description: "Find saved contexts and read exact files, provider records, and earlier conversations"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
