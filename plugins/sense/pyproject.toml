[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "sense"
version = "0.1.13"
description = "Keep one private, user-controlled work profile available across AI tools"
authors = [
  {name = "Ruzzy77"},
]
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
  "mcp>=2,<3",
]

[project.scripts]
sense = "sense.cli:main"
sense-mcp = "sense.mcp_server:main"

[dependency-groups]
dev = [
  "pillow>=11,<13",
  "pytest>=8,<10",
  "pyyaml>=6,<7",
]

[tool.setuptools.packages.find]
where = ["src"]
